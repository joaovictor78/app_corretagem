<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.corretagemapp">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CorretagemApp">

        <activity
            android:name=".homeActivity"
            android:label="Angel"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".calendar"
            android:label="Compromissos"
            android:parentActivityName=".homeActivity" />
        <activity
            android:name=".Cotacao"
            android:label="Minhas cotações"
            android:parentActivityName=".homeActivity"/>
        <activity android:name=".ValorTotalCotacao"
            android:label="Comprovante"
            android:parentActivityName=".homeActivity"/>

    </application>

</manifest>