<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="view.Cotacao">

             <androidx.recyclerview.widget.RecyclerView
                 android:id="@+id/lista_cotacao"
                 android:layout_width="match_parent"
                 android:layout_height="match_parent"
                 app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                 android:layout_marginTop="20dp" />
              <TextView
                  android:id="@+id/textEmpty"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:text="Nenhuma cotação encontrada! Adicione novas cotações."
                  android:textColor="@color/black"
                  android:layout_centerInParent="true"/>

           <LinearLayout
               android:id="@+id/linearLayout"
               android:layout_width="match_parent"
               android:layout_height="wrap_content"
               android:layout_alignParentBottom="true"
               android:orientation="horizontal"
               android:layout_margin="25dp"
               app:layout_constraintBottom_toBottomOf="parent">

               <Button
                   android:id="@+id/adicionar_pessoa"
                   android:layout_width="0dp"
                   android:layout_height="wrap_content"
                   android:layout_margin="10dp"
                   android:layout_weight="1"
                   android:background="@drawable/custom_bottomblack"
                   android:text="ADICIONAR"
                   android:textSize="15sp"
                   app:backgroundTint="#050000" />

               <Button
                   android:id="@+id/calcular_total"
                   android:layout_width="0dp"
                   android:layout_height="wrap_content"
                   android:layout_margin="10dp"
                   android:layout_weight="1"
                   android:background="@drawable/custom_bottom_orange"
                   android:text="CALCULAR"
                   android:textSize="15sp"
                   app:backgroundTint="#FB8500" />

           </LinearLayout>


</RelativeLayout>

